import React from 'react';

const History: React.FC = () => {
    return (
        <div className="p-6">
            <h2>Historical Log</h2>
            <p>A tabular log of every prediction returned by the active Random Forest pipeline.</p>
        </div>
    );
};

export default History;
