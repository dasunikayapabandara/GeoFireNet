# GeoFireNet Performance Testing Documentation

This document outlines the performance testing strategy for the GeoFireNet system. Performance testing focuses on the system's responsiveness, throughput, and stability under load, specifically evaluating ML inference latency and database query efficiency.

## Load and Latency Testing

| Test Case ID | Test Scenario | Test Objective | Preconditions | Test Steps | Expected Result | Actual Result | Status | Priority | Notes / Recommendation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **PT-001** | ML Inference Latency | Verify the Random Forest model inference time is minimal. | Backend is running with loaded artifacts. | 1. Send 100 sequential POST requests to `/predict`. 2. Measure the latency of each. | Average response time is < 50ms per prediction. | Pending execution | Pending | High | Ensures real-time capability. |
| **PT-002** | API Throughput (Requests/Sec) | Verify the API can handle concurrent dashboard requests. | Backend is running locally. | 1. Use Apache Bench (`ab -n 1000 -c 50`). 2. Target the `/health` endpoint. | API handles 50 concurrent requests without dropping connections or 500 errors. | Pending execution | Pending | High | Load baseline. |
| **PT-003** | History Endpoint Pagination | Verify database retrieval scales with large datasets. | Database seeded with 10,000 predictions. | 1. Request `/history?limit=50`. | Response returned in < 100ms. | Pending execution | Pending | Medium | Ensures UI remains snappy as DB grows. |
| **PT-004** | Global Summary Aggregation | Verify `/analytics/global_summary` performs efficiently. | Database seeded with 10,000 records. | 1. Send GET request to `/analytics/global_summary`. | Aggregation query executes and returns in < 200ms. | Pending execution | Pending | High | Key dashboard metric. |
| **PT-005** | Filtered Analytics Queries | Verify database indexing optimizes country queries. | DB seeded with 10,000 records across countries. | 1. Request `/analytics/global_summary?country=Australia`. | Query uses indexes, returns in < 150ms. | Pending execution | Pending | High | Validates DB schema design. |
| **PT-006** | Frontend Dashboard Render Time | Verify the React UI renders quickly on initial load. | Dashboard served by Vite. | 1. Open Chrome DevTools > Performance. 2. Reload dashboard. | First Contentful Paint (FCP) is < 1.0s. | Pending execution | Pending | Medium | User experience baseline. |
| **PT-007** | Chart Rendering Performance | Verify Chart.js handles large datasets without lagging. | History contains 365 data points. | 1. Load the Analytics page viewing 1 year of data. | Chart renders smoothly without freezing the browser thread. | Pending execution | Pending | Medium | Visualization stress test. |
| **PT-008** | Leaflet Map Marker Clustering | Verify map performance with hundreds of active detections. | DB seeded with 500 active detections. | 1. Open Live Map. 2. Zoom in/out. | Map pans and zooms at 60 FPS without stuttering. Markers cluster appropriately. | Pending execution | Planned | High | Map optimization. |
| **PT-009** | Weather API Latency | Verify integration with Open-Meteo doesn't block the backend. | External network is stable. | 1. Send 10 concurrent requests to `/weather/current`. | Backend handles concurrent IO awaits efficiently. Max latency < 800ms. | Pending execution | Pending | High | External dependency test. |
| **PT-010** | Database Connection Pooling | Verify SQLAlchemy handles multiple concurrent connections. | PostgreSQL default max connections (100). | 1. Simulate 50 concurrent read/write users. | No "too many clients" errors. Connection pool manages resources. | Pending execution | Pending | High | DB configuration test. |
| **PT-011** | Alerts Summary Caching | Verify the in-memory cache improves response times. | API running. | 1. Request `/alerts/summary` 10 times rapidly. | First request takes ~50ms. Subsequent 9 requests take < 5ms. | Pending execution | Pending | High | Caching logic validation. |
| **PT-012** | Cache Expiration/Invalidation | Verify the cache refreshes after 60 seconds. | API running. | 1. Wait 61 seconds. 2. Request `/alerts/summary`. | Request queries DB (takes ~50ms) instead of returning stale cache. | Pending execution | Pending | High | Data freshness validation. |
| **PT-013** | Alert Generation Overhead | Verify adding alert logic to prediction doesn't slow it down. | Pipeline active. | 1. Send 100 "Extreme" predictions. 2. Measure latency. | Average latency remains < 100ms despite DB writes to the `alerts` table. | Pending execution | Pending | High | Critical path optimization. |
| **PT-014** | Frontend Memory Leak Check | Verify the UI doesn't consume excessive memory over time. | Dashboard auto-refreshing every 30s. | 1. Leave dashboard open for 1 hour. 2. Monitor Chrome Memory tab. | JS Heap size remains stable (e.g., < 100MB), no unbounded growth. | Pending execution | Planned | Low | SPA longevity test. |
| **PT-015** | Startup Time | Verify the backend boots quickly for horizontal scaling. | FastAPI environment. | 1. Run `time python -m uvicorn backend.main:app`. | Server is ready to accept connections in < 3.0s. | Pending execution | Pending | Low | Deployment efficiency. |
| **PT-016** | ML Model Loading Time | Verify `joblib.load()` overhead during startup. | Large model files. | 1. Time the `load_model` function during boot. | Artifacts are loaded into RAM in < 1.0s. | Pending execution | Pending | Medium | Operational profiling. |
| **PT-017** | WebSocket Broadcast Latency | Verify real-time updates propagate instantly. | WebSocket connected. | 1. Trigger an alert. 2. Measure time until UI updates. | Alert appears in UI in < 100ms from DB commit. | Pending execution | Planned | High | Real-time requirement. |
| **PT-018** | Large Payload Handling | Verify the API rejects or handles excessively large JSON payloads. | FastAPI. | 1. Send a 10MB JSON body to `/predict`. | API rejects with 413 Payload Too Large or processes gracefully without crashing. | Pending execution | Planned | Low | Edge case testing. |
| **PT-019** | Concurrent Alert Resolution | Verify DB handles simultaneous patch requests on the same row. | Two clients. | 1. Send PATCH to `/alerts/1/resolve` simultaneously from two threads. | DB handles lock gracefully; one succeeds, the other might return conflict or success. | Pending execution | Planned | Medium | Concurrency testing. |
| **PT-020** | Cold Start Weather Request | Verify the first weather API request after idle handles DNS lookup. | After 10 mins idle. | 1. Request `/weather/current`. | Response is slightly slower but still < 2.0s without timing out. | Pending execution | Pending | Low | Network profiling. |
| **PT-021** | Static Asset Serving | Verify Vite serves bundled assets efficiently. | Production build. | 1. Inspect Network tab on initial load. | CSS/JS bundles are minified and < 500KB total. | Pending execution | Pending | Medium | Frontend optimization. |
| **PT-022** | Long-Polling Fallback | Verify performance if WebSockets fail and app falls back to polling. | WebSockets disabled. | 1. Monitor network traffic. | App sends requests every 30s, overhead is negligible (< 10KB/req). | Pending execution | Pending | Medium | Graceful degradation. |
| **PT-023** | Database Seeding Speed | Verify test data generation is efficient for CI/CD pipelines. | `seed_db.py`. | 1. Time the execution of `seed_db.py`. | 500 records are generated and committed in < 2.0s. | Pending execution | Pending | Low | DX (Developer Experience). |
| **PT-024** | React Virtual DOM Diffing | Verify UI doesn't re-render components unnecessarily. | React DevTools. | 1. Highlight updates. 2. Wait for auto-refresh. | Only changed components (e.g., Risk value text) flash; header/sidebar do not re-render. | Pending execution | Planned | Medium | Frontend profiling. |
| **PT-025** | Database Index Usage | Verify specific queries hit indexes instead of Seq Scans. | PostgreSQL `EXPLAIN ANALYZE`. | 1. Run `EXPLAIN ANALYZE` on the `/history` query. | Output shows `Index Scan` on timestamp and location_id. | Pending execution | Planned | High | DBA validation. |
| **PT-026** | Extreme Load Recovery | Verify the system recovers after CPU is pinned at 100%. | Load testing tool. | 1. Overload API until it drops requests. 2. Stop load. | API returns to normal sub-100ms latency within 5 seconds. | Pending execution | Planned | High | Resiliency test. |
| **PT-027** | Disk I/O during Prediction | Verify logging predictions doesn't bottleneck on disk write speed. | Slow disk simulated. | 1. Run predictions on a heavily utilized disk. | DB transaction buffers efficiently, API latency remains acceptable. | Pending execution | Planned | Low | Infrastructure independence. |
| **PT-028** | Memory Footprint (Idle) | Verify the backend doesn't hog RAM when not in use. | API running, no traffic. | 1. Check `htop` or Docker stats. | Uvicorn worker consumes < 150MB RAM. | Pending execution | Pending | Low | Resource efficiency. |
| **PT-029** | Memory Footprint (Load) | Verify the ML model doesn't leak memory per prediction. | After 10,000 predictions. | 1. Compare RAM to idle state. | RAM usage remains stable; Python garbage collection handles objects. | Pending execution | Pending | High | Long-running process stability. |
| **PT-030** | Dashboard Bundle Size | Verify the production build is optimized. | `npm run build`. | 1. Check Vite build output sizes. | Vendor chunks are split, total JS is < 500KB gzipped. | Pending execution | Pending | Medium | Web performance standards. |

## End of Performance Testing Document
