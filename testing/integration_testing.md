# GeoFireNet Integration Testing Documentation

This document contains integration test cases designed to verify that the distinct components of the GeoFireNet system—Frontend, API, Database, Machine Learning Model, and External Services—communicate and function correctly as a unified system.

## End-to-End System Integration

| Test Case ID | Test Scenario | Test Objective | Preconditions | Test Steps | Expected Result | Actual Result | Status | Priority | Notes / Recommendation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **IT-001** | Frontend-to-Backend HTTP Connection | Verify the React app can successfully negotiate a connection with FastAPI. | Both servers running. | 1. Open Dashboard. 2. Observe Network tab. | Dashboard successfully fetches `/health` and `/system/status` without CORS errors. | Pending execution | Pending | Critical | Core connectivity. |
| **IT-002** | Weather Provider to ML Pipeline | Verify Open-Meteo data is successfully formatted and fed into the Random Forest model. | External network active. | 1. Submit prediction via UI for "Napa Valley". | API fetches live weather, parses it to floats, feeds it to ML model without type errors, and returns a valid Risk Score. | Pending execution | Pending | Critical | Primary data flow. |
| **IT-003** | ML Predictor to PostgreSQL Logging | Verify the output of the ML model is successfully persisted via SQLAlchemy. | Database running. | 1. Submit prediction. 2. Query DB. | The exact risk score, probability, and derived risk level output by the model are saved in `risk_prediction_logs`. | Pending execution | Pending | High | Traceability. |
| **IT-004** | PostgreSQL Logging to Alert Pipeline | Verify a saved "Extreme" prediction triggers the Alert Service. | Alert thresholds configured. | 1. Force an extreme prediction. | Prediction is saved, THEN an Alert row is created in the DB with a foreign key linking back to the Prediction ID. | Pending execution | Pending | High | Event-driven architecture. |
| **IT-005** | Alert Pipeline to Frontend Rendering | Verify generated alerts appear immediately on the Dashboard UI. | Dashboard open. | 1. Generate an alert via API. 2. Wait for UI auto-refresh. | The new alert appears in the "Recent Alerts" list and the Active Alerts count increments. | Pending execution | Pending | High | End-to-end visibility. |
| **IT-006** | UI Filter to Database Query | Verify the Dashboard country dropdown correctly translates to SQL queries. | Dashboard open. | 1. Select "Australia" from the UI dropdown. | UI sends `?country=Australia` to API, which executes a filtered `JOIN` on the Location table, returning specific data. | Pending execution | Pending | High | Full stack parameter passing. |
| **IT-007** | UI Resolve Button to Database Update | Verify clicking "Resolve" in the UI updates the PostgreSQL state. | UI Alert list open. | 1. Click "Resolve" on an active alert. | UI sends PATCH request. DB updates `status` to `resolved`. UI refreshes to show the new state. | Pending execution | Pending | High | State mutation flow. |
| **IT-008** | Database Seed to Frontend Analytics | Verify seeded historical data correctly renders on the Risk Trend chart. | Run `seed_db.py`. | 1. Open Dashboard Analytics. | The generated historical points form an aggregated line chart corresponding to the seeded timestamps. | Pending execution | Pending | Medium | Data visualization integrity. |
| **IT-009** | Missing ML Model Fallback | Verify how the Frontend handles a broken ML-Backend link. | Delete `model.joblib`. | 1. Load Dashboard. | Backend `/system/status` reports degraded model. Frontend disables the "Predict" button or shows a specific service error. | Pending execution | Planned | High | Graceful degradation. |
| **IT-010** | Database Outage to Frontend Error | Verify how the Frontend handles a broken Backend-DB link. | Stop PostgreSQL. | 1. Load Dashboard. | Backend `/health` catches DB error. Frontend displays a critical "Backend unreachable or degraded" banner. | Pending execution | Pending | High | Observability. |
| **IT-011** | Location Coordinates to Map Rendering | Verify stored Lat/Lon values translate correctly to Leaflet map markers. | Seed DB with coordinates. | 1. Open Live Map. | The coordinates fetched from `/history` are plotted exactly at the expected geographic locations on the base map. | Pending execution | Pending | Medium | Spatial integration. |
| **IT-012** | Model Versioning Linking | Verify a prediction is linked to the active model version in the database. | `model_versions` table populated. | 1. Submit prediction. | The new row in `risk_prediction_logs` has a valid `model_version_id` linking to the currently active model. | Pending execution | Planned | Low | Auditing requirement. |
| **IT-013** | SQLAlchemy Cascade Delete | Verify deleting a location cascades to predictions and alerts. | DB populated. | 1. Delete a Location via CRUD. | All Predictions and Alerts associated with that Location are deleted to prevent orphaned foreign keys. | Pending execution | Planned | Medium | Relational integrity. |
| **IT-014** | Form Validation to API Validation | Verify frontend React validation aligns with backend Pydantic validation. | Submit Form. | 1. Bypass frontend JS validation and send `humidity: 150`. | Pydantic catches what the frontend missed and returns 422. The schemas match identically. | Pending execution | Pending | High | Defense in depth. |
| **IT-015** | WebSocket Event Publishing | Verify an API state change broadcasts to connected WebSocket clients. | WebSocket enabled. | 1. Resolve an alert via API. | The backend WebSocket manager broadcasts a `{"type": "alert_resolved", "id": 1}` message to all connected React clients. | Pending execution | Planned | Medium | Real-time push integration. |
| **IT-016** | Frontend Polling Sync | Verify multiple open browser tabs stay in sync via polling. | Open 2 Dashboard tabs. | 1. Resolve alert in Tab A. 2. Wait 30s. | Tab B automatically fetches the updated state and hides the resolved alert without manual refresh. | Pending execution | Pending | Medium | Multi-client consistency. |
| **IT-017** | Alembic Migration Integrity | Verify the SQLAlchemy models match the Alembic schema migrations exactly. | Run Alembic check. | 1. Run `alembic check` or `alembic revision --autogenerate`. | No changes detected, indicating the `models.py` and the database schema are perfectly synchronized. | Pending execution | Pending | Critical | ORM synchronization. |
| **IT-018** | Weather API Retry Logic | Verify the backend retries fetching weather if the first attempt fails. | Mock Open-Meteo. | 1. Block first weather request, allow second. | Backend retries internally and eventually succeeds, returning a prediction to the frontend without error. | Pending execution | Planned | Medium | Resiliency. |
| **IT-019** | Settings Theme to CSS Variables | Verify the React state toggle successfully rewrites the CSS root variables. | Click Theme Toggle. | 1. Toggle dark mode. | React updates state -> `useEffect` modifies `document.documentElement.setAttribute('data-theme')` -> CSS variables update -> App re-renders. | Pending execution | Pending | Low | DOM integration. |
| **IT-020** | Date Parsing Pipeline | Verify timestamps flow correctly from Postgres (UTC) -> FastAPI -> React (Local Time). | Submit Prediction. | 1. Generate prediction. 2. View in UI. | DB stores UTC. React parses ISO string and displays it in the user's local timezone (e.g., using `toLocaleTimeString`). | Pending execution | Pending | High | Globalization/Formatting. |
| **IT-021** | Error Stack Trace Masking | Verify the API doesn't send Python stack traces to the React frontend. | Cause 500 error. | 1. Trigger backend bug. 2. Check React Network tab. | Response is a clean JSON error object. React parses it and displays a user-friendly "Something went wrong" toast. | Pending execution | Pending | High | Security/UX integration. |
| **IT-022** | Pagination Integration | Verify the React UI correctly handles API pagination links/offsets. | UI pagination. | 1. Click "Next Page" in History. | React updates `offset=50` -> API queries DB `OFFSET 50 LIMIT 50` -> React renders the next batch seamlessly. | Pending execution | Planned | Medium | Data handling. |
| **IT-023** | Component Reusability | Verify the `RiskCard` component functions identically when fed global vs. country data. | Change filters. | 1. Toggle between Global and Australia. | The component accepts the distinct props and renders correctly without internal logic errors. | Pending execution | Pending | Low | Frontend architecture. |
| **IT-024** | Empty State Full Stack | Verify an empty database renders cleanly in the UI. | Truncate DB. | 1. Load Dashboard. | API returns `[]`. React maps over it safely and triggers the visual Empty State components. | Pending execution | Pending | High | Edge case integration. |
| **IT-025** | Threshold Configuration Sync | Verify the thresholds used by the Alert Service match the ML evaluation output. | Configuration check. | 1. Check `alert_service.py` vs `thresholds.json`. | The Alert Service dynamically loads from `thresholds.json` rather than hardcoding values, keeping them in sync with ML retraining. | Pending execution | Pending | High | System calibration. |
| **IT-026** | Chart.js Data Mapping | Verify the custom API `RiskChartData` structure parses perfectly into Chart.js. | Load Analytics. | 1. View Risk Trend Chart. | The `labels` and `datasets` arrays map directly to the Chart.js configuration object without structural errors. | Pending execution | Pending | Medium | UI Library integration. |
| **IT-027** | Leaflet Coordinate Projection | Verify Lat/Lon floats from the API project accurately to the Web Mercator map. | Load Map. | 1. Inspect marker position. | The marker is positioned correctly relative to the geographic boundaries of the Leaflet tiles. | Pending execution | Pending | Medium | GIS integration. |
| **IT-028** | Environment Variable Flow | Verify `.env` variables flow into both Vite and FastAPI correctly. | Setup. | 1. Start system. | Vite uses `VITE_API_BASE_URL`. FastAPI uses `DATABASE_URL`. They don't cross-contaminate. | Pending execution | Pending | High | Configuration management. |
| **IT-029** | Graceful Connection Drop | Verify React handles a dropped connection mid-request. | Throttle connection. | 1. Start prediction. 2. Kill network immediately. | `fetch` throws a TypeError. React catches it and displays "Network Error" rather than freezing. | Pending execution | Pending | Medium | Network integration. |
| **IT-030** | E2E Happy Path | Verify a complete end-to-end user journey succeeds. | Fresh start. | 1. User loads Dashboard. 2. Submits Prediction for Napa. 3. Risk is Extreme. 4. Alert is generated. 5. User views Map. 6. User resolves Alert. | The entire sequence completes without a single console error or UI glitch. | Pending execution | Pending | Critical | Final Acceptance. |

## End of Integration Testing Document
