# GeoFireNet ML Accuracy Evaluation Testing

This document contains test cases focused strictly on the performance, calibration, and accuracy of the underlying Random Forest machine learning model. The objective is to validate that the model makes statistically sound and safe predictions for wildfire risk.

## Machine Learning Validation and Calibration

| Test Case ID | Test Scenario | Test Objective | Preconditions | Test Steps | Expected Result | Actual Result | Status | Priority | Notes / Recommendation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **ML-001** | High Recall Verification | Verify the model prioritizes recall over precision to minimize false negatives. | Evaluated against a test dataset. | 1. Run `evaluate_model.py`. 2. Check Recall metric for Class 1 (Fire). | Recall is consistently >= 0.90 across cross-validation folds. | Pending execution | Pending | Critical | Missing a fire is more dangerous than a false alarm. |
| **ML-002** | Extreme Weather Baseline | Verify the model correctly identifies textbook extreme fire weather. | Model loaded via API. | 1. Predict with `temp: 45°C`, `hum: 5%`, `wind: 60km/h`, `veg: 0.05`. | Risk probability is > 0.90 (Extreme). | Pending execution | Pending | High | Fundamental physical bounds test. |
| **ML-003** | Benign Weather Baseline | Verify the model correctly identifies textbook safe weather. | Model loaded via API. | 1. Predict with `temp: 5°C`, `hum: 95%`, `wind: 2km/h`, `veg: 0.95`. | Risk probability is < 0.10 (Low). | Pending execution | Pending | High | Fundamental physical bounds test. |
| **ML-004** | Feature Importance Validity | Verify Temperature and Vegetation Moisture are the primary drivers. | Model trained. | 1. Inspect `feature_importances_` array from the Random Forest. | `temp` and `veg_moisture` combined account for > 50% of the total importance. | Pending execution | Pending | Medium | Aligns ML with physical meteorology. |
| **ML-005** | Humidity Inversion Test | Verify risk decreases as humidity increases, holding others constant. | Model loaded via API. | 1. Predict at 10% humidity. 2. Predict at 90% humidity. | Prediction 1 Risk > Prediction 2 Risk. | Pending execution | Pending | High | Monotonicity check. |
| **ML-006** | Wind Speed Scaling | Verify risk increases non-linearly with extreme wind speeds. | Model loaded via API. | 1. Predict at 10km/h wind. 2. Predict at 100km/h wind. | Prediction 2 Risk is significantly higher than Prediction 1. | Pending execution | Pending | High | Monotonicity check. |
| **ML-007** | Vegetation Moisture Impact | Verify low vegetation moisture drastically spikes risk even at moderate temps. | Model loaded via API. | 1. Predict `temp: 20`, `veg: 0.1`. | Risk is classified as High or Extreme despite moderate temperature. | Pending execution | Pending | High | Domain-specific interaction check. |
| **ML-008** | Threshold Calibration (Moderate) | Verify the `calibrate_thresholds.py` script correctly sets the Moderate boundary. | Script run. | 1. Check generated `thresholds.json`. | Moderate threshold is around 0.30 - 0.40 probability. | Pending execution | Pending | Medium | Ensures dynamic binning works. |
| **ML-009** | Threshold Calibration (High) | Verify the High risk boundary accurately isolates top 20% of risks. | Script run. | 1. Check generated `thresholds.json`. | High threshold is around 0.60 - 0.70 probability. | Pending execution | Pending | Medium | Ensures dynamic binning works. |
| **ML-010** | Threshold Calibration (Extreme) | Verify the Extreme risk boundary targets the 95th percentile. | Script run. | 1. Check generated `thresholds.json`. | Extreme threshold is >= 0.85 probability. | Pending execution | Pending | High | Ensures alert fatigue is minimized. |
| **ML-011** | Cold Weather Fire | Verify model handles dry/windy but cold weather (e.g., winter fires). | Model loaded via API. | 1. Predict `temp: 5°C`, `hum: 10%`, `wind: 50km/h`, `veg: 0.1`. | Risk is Moderate to High, not Low. | Pending execution | Pending | Medium | Edge case evaluation. |
| **ML-012** | Humid Heatwave | Verify model handles hot but extremely humid environments (tropics). | Model loaded via API. | 1. Predict `temp: 40°C`, `hum: 95%`, `wind: 5km/h`, `veg: 0.8`. | Risk is Low to Moderate, not Extreme. | Pending execution | Pending | Medium | Edge case evaluation. |
| **ML-013** | False Positive Rate Tolerance | Verify the model's FPR is acceptable for an alert system. | Evaluated against test dataset. | 1. Run evaluation script. | False Positive Rate is < 0.20 to prevent overwhelming analysts. | Pending execution | Pending | High | Operational usability metric. |
| **ML-014** | Cross-Validation Stability | Verify the model doesn't overfit to specific training folds. | Stratified K-Fold CV. | 1. Check standard deviation of ROC-AUC across 5 folds. | Std Dev of AUC is < 0.05. | Pending execution | Pending | High | Generalization capability. |
| **ML-015** | ROC-AUC Score Target | Verify the model has high discriminative ability. | Evaluated against test dataset. | 1. Run evaluation script. | ROC-AUC score is >= 0.90. | Pending execution | Pending | High | Academic/ML baseline metric. |
| **ML-016** | Zero Variance Inputs | Verify model behavior when all inputs are 0. | Model loaded via API. | 1. Predict `temp: 0`, `hum: 0`, `wind: 0`, `veg: 0`. | Returns a calculated probability without math errors (divide by zero). | Pending execution | Pending | Low | API bounds testing. |
| **ML-017** | Negative Temperature Handling | Verify model handles sub-zero temperatures correctly. | Model loaded via API. | 1. Predict `temp: -20°C`. | Model computes successfully; Risk is Low. | Pending execution | Pending | Low | Input domain validation. |
| **ML-018** | Out of Distribution: Extreme Wind | Verify model handles hurricane-force winds beyond training data. | Model loaded via API. | 1. Predict `wind: 250km/h`. | Model handles it gracefully, maxes out risk probability without crashing. | Pending execution | Pending | Medium | OOD robustness. |
| **ML-019** | Model Version Mismatch | Verify API rejects loading an incompatible `joblib` structure. | Swap `.joblib` with an old Sklearn version. | 1. Start backend. | API fails to load model and falls back to degraded mode, logging an error. | Pending execution | Planned | High | Deployment safety. |
| **ML-020** | Random Forest Tree Count | Verify the ensemble size is sufficient but performant. | `train_model.py`. | 1. Inspect `n_estimators`. | Value is between 100 and 300 to balance accuracy and inference speed. | Pending execution | Pending | Medium | Hyperparameter validation. |
| **ML-021** | Training Data Class Imbalance | Verify SMOTE or class weighting is applied during training. | `train_model.py`. | 1. Check training logs. | `class_weight='balanced'` is used or synthetic data generated to handle rare fire events. | Pending execution | Pending | High | Crucial for high recall. |
| **ML-022** | Feature Scaling Independence | Verify Random Forest doesn't require StandardScaling. | Code review. | 1. Check ML pipeline. | Input features are passed directly to RF without a `StandardScaler`, as trees are scale-invariant. | Pending execution | Pending | Low | Architectural validation. |
| **ML-023** | Consistent Output Bounds | Verify `predict_proba` always returns a float between 0.0 and 1.0. | API running. | 1. Run 1000 random permutations. | Output is strictly `0.0 <= prob <= 1.0`. | Pending execution | Pending | High | Mathematical correctness. |
| **ML-024** | Deterministic Inference | Verify the same input always yields the exact same probability. | API running. | 1. Predict identical payload 5 times. | All 5 predictions return the exact same 4-decimal risk score. | Pending execution | Pending | High | Production determinism. |
| **ML-025** | F1-Score Evaluation | Verify the harmonic mean of precision and recall is strong. | Evaluation script. | 1. Check F1-Score. | F1-Score is >= 0.85, indicating a good balance despite class imbalance. | Pending execution | Pending | Medium | Statistical validity. |
| **ML-026** | Confusion Matrix Logging | Verify the evaluation script outputs a clear confusion matrix. | Evaluation script. | 1. Check `ml_evaluation_report.md`. | True Positives, False Positives, True Negatives, and False Negatives are explicitly listed. | Pending execution | Pending | Medium | Reporting requirement. |
| **ML-027** | Feature Name Consistency | Verify the API input matches the model's expected column names. | API / Model integration. | 1. Inspect `prediction_service.py`. | API `temp` -> Model `Temperature`, `hum` -> `Humidity`, etc., mapped correctly. | Pending execution | Pending | High | Integration validation. |
| **ML-028** | Real-time Weather Override | Verify the model can accept manual inputs ignoring Open-Meteo. | Dashboard Prediction. | 1. Check "Manual Override" or type custom values. | The model calculates based entirely on the typed values, ignoring live weather. | Pending execution | Pending | Medium | Flexibility testing. |
| **ML-029** | Missing Thresholds File | Verify system uses safe fallback thresholds if `thresholds.json` is missing. | Delete `thresholds.json`. | 1. Start backend. | Hardcoded safe defaults (e.g., Extreme=0.85) are used. | Pending execution | Planned | High | Graceful degradation. |
| **ML-030** | Database Precision Storage | Verify the DB accurately stores the ML float outputs without truncation loss. | DB Schema. | 1. Check `risk_probability` column. | Stored as `Float` or `Numeric(p,s)` ensuring at least 4 decimal places of accuracy. | Pending execution | Pending | Low | Data integrity. |

## End of ML Accuracy Testing Document
