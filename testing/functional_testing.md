# GeoFireNet Functional Testing Documentation

This document contains comprehensive functional test cases for the GeoFireNet system. Functional testing ensures that the application operates according to the specified requirements, specifically focusing on the ML inference pipeline, database persistence, REST API endpoints, and React frontend functionality.

## Core Feature Testing

| Test Case ID | Test Scenario | Test Objective | Preconditions | Test Steps | Test Data | Expected Result | Actual Result | Status | Priority | Notes / Recommendation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **FN-001** | Backend Initialization | Verify backend starts successfully with loaded ML artifacts. | ML `.joblib` files exist in `backend/artifacts`. | 1. Start the API using `uvicorn`. 2. Monitor standard output. | N/A | Server starts on port 8000. Logs indicate "Predictor ML artifacts loaded successfully." | Pending execution | Pending | High | Critical startup dependency. |
| **FN-002** | Database Connectivity | Verify the API connects to the PostgreSQL database on startup. | Valid `.env` configuration. PostgreSQL server is running. | 1. Start the API. 2. Send GET request to `/health`. | N/A | JSON response includes `"database": "healthy"`. | Pending execution | Pending | High | Needed for CRUD operations. |
| **FN-003** | API Health Check | Verify `/health` endpoint returns full diagnostic schema. | API is running. | 1. Navigate to `http://localhost:8000/health`. | N/A | Returns status, version, api, database, model, and timestamp fields. | Pending execution | Pending | High | Used by dashboard connection retry. |
| **FN-004** | System Status Endpoint | Verify `/system/status` returns aggregated system health. | API is running. | 1. Navigate to `http://localhost:8000/system/status`. | N/A | Returns JSON with `status`, `components.database`, and `components.model`. | Pending execution | Pending | Medium | Used by the UI. |
| **FN-005** | Real-Time Weather Ingestion | Verify Open-Meteo integration fetches current weather successfully. | API has network access. | 1. Send GET request to `/weather/current?lat=38.5&lon=-122.3`. | lat: `38.5`, lon: `-122.3` (Napa Valley) | Returns success status with `temp`, `humidity`, `wind`, and `veg_moisture`. | Pending execution | Pending | High | Requirement for manual predictions. |
| **FN-006** | Invalid Coordinate Weather Request | Verify error handling for out-of-bounds coordinates. | API is running. | 1. Send GET request to `/weather/current?lat=95&lon=200`. | lat: `95`, lon: `200` | Returns HTTP 400 or 422 Validation Error. | Pending execution | Pending | Medium | Ensures API robustness. |
| **FN-007** | Automated ML Prediction (Low Risk) | Verify the prediction endpoint calculates low risk for benign weather. | Model is loaded. | 1. Send POST to `/predict` with safe weather parameters. | temp: `15`, hum: `70`, wind: `5`, veg: `0.8` | Returns risk level "Low" (probability < 0.3). Prediction is logged in DB. | Pending execution | Pending | High | Core application feature. |
| **FN-008** | Automated ML Prediction (Extreme Risk) | Verify the prediction endpoint identifies high-risk scenarios. | Model is loaded. | 1. Send POST to `/predict` with dangerous weather parameters. | temp: `40`, hum: `10`, wind: `35`, veg: `0.1` | Returns risk level "Extreme" (probability > 0.8). Prediction logged in DB. | Pending execution | Pending | High | Safety-critical feature. |
| **FN-009** | Prediction Form UI Submission | Verify the dashboard form correctly submits a prediction request. | Dashboard and API running. | 1. Navigate to Predictions page. 2. Enter location and weather data. 3. Click Submit. | Location: "California", temp: 30, hum: 20 | Frontend displays the resulting risk score and level without errors. | Pending execution | Pending | High | Primary user interaction. |
| **FN-010** | Prediction Form Validation | Verify the frontend enforces input constraints. | Dashboard is running. | 1. Navigate to Predictions page. 2. Enter negative humidity. 3. Click Submit. | hum: `-10` | Form prevents submission. UI displays "Humidity must be between 0 and 100". | Pending execution | Pending | Medium | Prevents bad data entry. |
| **FN-011** | Prediction Persistence | Verify that a generated prediction is saved to PostgreSQL. | Valid prediction submitted. | 1. Submit prediction. 2. Query `risk_prediction_logs` table. | Location: "Greece" | Record exists matching the input parameters and output risk score. | Pending execution | Pending | High | Required for historical trend tracking. |
| **FN-012** | Alert Generation Threshold | Verify alerts are automatically triggered for Extreme predictions. | Prediction pipeline active. | 1. Submit Extreme risk prediction. 2. Query `/alerts` endpoint. | temp: 42, hum: 5 | Alert is returned with severity "extreme" and status "active". | Pending execution | Pending | High | Core alerting feature. |
| **FN-013** | Alert Non-Generation Threshold | Verify alerts are NOT generated for Low risk predictions. | Prediction pipeline active. | 1. Submit Low risk prediction. 2. Query `/alerts` endpoint. | temp: 10, hum: 90 | No new alert is generated in the system. | Pending execution | Pending | High | Prevents alert fatigue. |
| **FN-014** | Acknowledge Alert API | Verify the API can transition an alert to "acknowledged" state. | An active alert exists. | 1. Send PATCH to `/alerts/{id}/acknowledge`. | `alert_id: 1` | Returns alert with `status: "acknowledged"`. Timestamp updated in DB. | Pending execution | Pending | High | Incident response workflow. |
| **FN-015** | Resolve Alert API | Verify the API can transition an alert to "resolved" state. | An active alert exists. | 1. Send PATCH to `/alerts/{id}/resolve`. | `alert_id: 1` | Returns alert with `status: "resolved"`. Timestamp updated in DB. | Pending execution | Pending | High | Incident response workflow. |
| **FN-016** | Invalid Alert ID Resolution | Verify resolving a non-existent alert handles gracefully. | API is running. | 1. Send PATCH to `/alerts/99999/resolve`. | `alert_id: 99999` | Returns HTTP 404 Not Found. | Pending execution | Pending | Low | Prevents DB errors. |
| **FN-017** | Global Alerts Summary | Verify `/alerts/summary` returns accurate aggregated counts. | DB contains multiple alerts. | 1. Send GET to `/alerts/summary`. | N/A | JSON matches the exact count of active, high, and extreme alerts in the DB. | Pending execution | Pending | Medium | Used for KPI cards. |
| **FN-018** | Filtered Alerts Summary | Verify `/alerts/summary?country=X` returns filtered counts. | DB contains alerts for USA and Australia. | 1. Send GET to `/alerts/summary?country=Australia`. | country: `Australia` | JSON reflects only the active alerts belonging to Australia. | Pending execution | Pending | High | Resolves caching bleeding bug. |
| **FN-019** | Empty Country Alerts Summary | Verify filtering for a country with no data returns zeros. | DB has no data for Greece. | 1. Send GET to `/alerts/summary?country=Greece`. | country: `Greece` | `active_total: 0`, `active_high: 0`, `active_extreme: 0`. | Pending execution | Pending | High | Validates empty states. |
| **FN-020** | Fetch History List | Verify `/history` returns the most recent predictions. | DB has predictions. | 1. Send GET to `/history`. | N/A | Returns an array of prediction logs ordered by timestamp descending. | Pending execution | Pending | Medium | History tab functionality. |
| **FN-021** | Delete History Item | Verify `/history/{id}` DELETE method removes the record. | DB has a prediction with ID 1. | 1. Send DELETE to `/history/1`. 2. Send GET to `/history/1`. | `id: 1` | DELETE returns success. Subsequent GET returns HTTP 404. | Pending execution | Pending | Medium | CRUD completeness. |
| **FN-022** | Cascading Delete on History | Verify deleting a prediction also deletes its associated alert. | Prediction 2 has an active alert. | 1. Send DELETE to `/history/2`. 2. Check alerts table. | `id: 2` | The associated alert is automatically deleted via SQLAlchemy cascade. | Pending execution | Pending | High | Maintains relational integrity. |
| **FN-023** | Global Analytics Summary | Verify `/analytics/global_summary` returns grouped risk levels. | DB has varied predictions. | 1. Send GET to `/analytics/global_summary`. | N/A | JSON returns count array grouped by `risk_level` (Low, Moderate, High, Extreme). | Pending execution | Pending | Medium | Required for dashboard overview. |
| **FN-024** | Active Detections API | Verify `/detections` returns satellite active fire detections. | DB seeded with detection data. | 1. Send GET to `/detections`. | N/A | Returns array of `ActiveDetectionLog` entries. | Pending execution | Pending | Medium | Map integration data source. |
| **FN-025** | Country Selection UI Filter | Verify dashboard updates state when a country is selected. | Dashboard running. | 1. Select "Australia" from dropdown. | country: `Australia` | The UI clears stale data, triggers loading state, and fetches Australia-specific data. | Pending execution | Pending | High | Core navigation. |
| **FN-026** | Empty State UI Rendering | Verify charts and lists display placeholder when no data exists. | Country with no data selected. | 1. Select "Greece" from dropdown. | country: `Greece` | Risk Index says "No Data". Chart says "No prediction history available". | Pending execution | Pending | High | UX completeness. |
| **FN-027** | Live Map Rendering | Verify map component loads without crashing. | Dashboard running. | 1. Navigate to Live Map tab. | N/A | Leaflet map renders correctly, bounding box adjusts to global view. | Pending execution | Pending | High | Map dependency validation. |
| **FN-028** | Map Point Placements | Verify historical predictions appear as markers on the map. | DB contains predictions with lat/lon. | 1. Navigate to Live Map tab. 2. Observe markers. | N/A | Markers correspond to prediction coordinates. Marker colors match risk levels. | Pending execution | Pending | Medium | Spatial feature verification. |
| **FN-029** | Settings Theme Toggle | Verify theme toggle correctly switches application CSS variables. | Dashboard running. | 1. Navigate to Settings. 2. Click Theme Toggle. | N/A | App visual theme switches between light and dark modes successfully. | Pending execution | Pending | Low | Personalization feature. |
| **FN-030** | Database Seeding Script | Verify `seed_db.py` correctly populates an empty database. | Empty database. | 1. Run `python seed_db.py`. 2. Query tables. | N/A | Locations, Weather Inputs, Predictions, and Alerts tables contain realistic mock data. | Pending execution | Pending | Medium | Required for testing environment. |

## End of Functional Testing Document
