# Database Configuration
# Explicitly use PostgreSQL for the application (SQLite is restricted to isolated memory unit tests)
DATABASE_URL=postgresql://dasunika:geofirenet_dev@localhost:5432/geofirenet_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=dasunika
DB_PASSWORD=geofirenet_dev
DB_NAME=geofirenet_db

# Application Configuration
ENVIRONMENT=development
SIMULATE_OUTAGE=0
